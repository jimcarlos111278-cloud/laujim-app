# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Android build artifacts
android/.gradle/
android/build/
android/app/build/
android/captures/
android/local.properties

# APK files
app-debug.apk
public/app-debug.apk

# Photos / uploads
uploads/photos/*

# Runtime database snapshots. These are ephemeral local mirrors of the Aiven
# PostgreSQL store. Committing them ships a stale snapshot to every deploy and
# causes the app to "revert to an old state" when Aiven is unreachable/empty.
data/
backups/

# Session state
AGENTS.md
.session*

Proyecto Laujim APP - Render/

# Puppeteer / Chrome session data
sessions/

# Contiene credenciales de la base Aiven - no subir
restore-aiven.cjs

# Graphify - cache y backups regenerables (el reporte y graph.json si se versionan)
graphify-out/cache/
graphify-out/*/
graphify-out/graph.html